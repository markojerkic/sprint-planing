version: "2"
sql:
  - engine: "sqlite"
    # put queries in directory `queries`
    queries: "queries"
    schema: "internal/database/migrations"
    gen:
      go:
        package: "dbgen"
        out: "internal/database/dbgen"
